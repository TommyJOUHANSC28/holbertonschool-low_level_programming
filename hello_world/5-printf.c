#include <stdio.h>
/**
*main - Entry point
 *put the message "with proper grammar, but the outcome is a piece of art"
 *Return: Alvay 0 (Success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
